Arreglo /resumen — 2 archivos
================================

CAUSA (confirmada con el log que mandaste):

1) Gemini bloqueó el resumen por su propio filtro de contenido
   (blockReason: PROHIBITED_CONTENT) — probablemente algo puntual en
   los mensajes recientes del grupo lo disparó. Esto es una decisión de
   Gemini, no un bug del bot; no se puede "arreglar" del lado del
   código, pero sí mejoré el aviso (ver abajo).

2) El respaldo con Groq TAMBIÉN falló, y este sí era un bug real: el
   modelo que el bot tenía configurado por defecto
   (llama-3.3-70b-versatile) fue dado de baja por Groq el 16 de agosto
   de 2026. Por eso daba 404 Not Found — no era tu API key ni tu
   cuenta, era el nombre del modelo, que ya no existe más del lado de
   Groq.

QUÉ CAMBIÉ:

1. config.py (MODIFICADO)
   - Modelo de Groq actualizado de "llama-3.3-70b-versatile" a
     "openai/gpt-oss-120b" (el reemplazo que recomienda Groq).
   - Si en tu .env tenés GROQ_MODEL puesto a mano con el nombre viejo,
     sacalo o cambialo también ahí — el .env tiene prioridad sobre este
     valor por defecto.

2. handlers/gemini_chat.py (MODIFICADO)
   - Cuando Gemini bloquea el pedido por contenido, ahora se detecta
     específicamente (antes se perdía en un error genérico) y, si
     encima falla el respaldo de Groq, /resumen te avisa con el motivo
     real en vez de un "probá más tarde" sin explicación:
     "😅 Gemini bloqueó el resumen por su filtro de contenido (motivo:
     PROHIBITED_CONTENT)... probá con menos mensajes (/resumen 50) o
     más tarde."
   - Con el modelo de Groq ya arreglado, en la práctica esto debería
     dejar de pasar casi siempre: cuando Gemini bloquee, Groq va a
     poder responder igual (Groq suele ser más permisivo con ese tipo
     de contenido).

Probado sin depender de la red: reproduje el escenario exacto de tu log
(Gemini bloquea -> Groq también falla) y confirmé que el mensaje nuevo
identifica bien el motivo real.

IMPORTANTE: este zip NO incluye ningún archivo de base de datos
(bot.db). Nunca reemplaces database/bot.db con nada de un zip mío.
